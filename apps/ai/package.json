{
  "name": "ai",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run python main.py",
    "build": "uv sync",
    "lint": "uvx ruff check .",
    "start": "uv run uvicorn main:app --host 0.0.0.0 --port 8000"
  }
}
