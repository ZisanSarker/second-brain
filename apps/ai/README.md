# AI Second Brain — AI Worker Service

FastAPI microservice for chunking, embeddings, LLM calls, OCR, and vector search.

See the [root README](../../README.md) for installation, deployment, and usage instructions.

## Quick Reference

```bash
uv run uvicorn main:app --reload  # Start dev server on :8000
uv sync --no-dev                   # Install production deps
```
